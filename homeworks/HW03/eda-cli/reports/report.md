Кастомный отчёт

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Качество данных (эвристики)

- Оценка качества: **0.44**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Есть колонки, где все значения одинаковые: **False**
- Есть категориальные признаки с очень большим числом уникальных значений: **True**
- Есть дублирующиеся идентификаторы: **True**
- Есть числовые колонки с слишком большим содержанием нулевых значений: **False**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

Проблемные колонки с слишком большим содержанием пропусков (больше 0.01) были найдены: 
city
avg_session_duration_min


## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

См. топ 3 категориальных признаков в файлах в папке `top_categories/`.

## Гистограммы числовых колонок

Максимальное число колонок для гистограмм: 5
См. файлы `hist_*.png`.
